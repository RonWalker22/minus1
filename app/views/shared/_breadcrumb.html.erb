<nav class="breadcrumb is-centered" aria-label="breadcrumbs">
  <ul>
    <%= render "shared/breadcrumbs/game" if @game%> 
    <%= render "shared/breadcrumbs/strategy" if @strategy%> 
    <%= render "shared/breadcrumbs/objective" if @objective%> 
    <%= render "shared/breadcrumbs/recipe" if @recipe %>
    <%= render "shared/breadcrumbs/direction" if @direction %>
    <%= render "shared/breadcrumbs/ingredient" if @ingredient %>
  </ul>
</nav>