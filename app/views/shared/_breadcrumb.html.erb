<nav class="breadcrumb is-centered" aria-label="breadcrumbs">
  <ul>
    <li id="breadcrumb-strategy">
      <a href="<%= strategy_path(@strategy) %>">
        <span class="icon is-small">
          <i class="fas fa-sitemap" aria-hidden="true"></i>
        </span>
        <span>Strategy</span>
      </a>
    </li>
    <%= render "shared/breadcrumbs/objective"%>
    <%= render "shared/breadcrumbs/recipe" if @recipe %>
    <%= render "shared/breadcrumbs/ingredient" if @ingredient %>
  </ul>
</nav>